# PostgreSQL connection settings for the data warehouse.
# Copy this file to .env (which is gitignored) and adjust as needed.
POSTGRES_USER=valorant
POSTGRES_PASSWORD=valorant
POSTGRES_DB=valorant
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Target schema for raw loaded tables.
RAW_SCHEMA=raw
